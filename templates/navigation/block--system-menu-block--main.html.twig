{#
/**
 * @file block--system-menu-block--main.html.twig
 * Meadow Lane Park — Main navigation menu block.
 *
 * Drupal renders this block into the Primary Menu region, which the
 * site-header partial outputs as {{ page.primary_menu }} inside the
 * <ul class="nav__menu"> element.
 *
 * The block wrapper div is suppressed so only the <ul class="menu">
 * rendered by menu.html.twig lands inside the nav's flex container.
 */
#}
{{ content }}
